# This is a basic workflow that is manually triggered
name: variables and secrets
env:
WF_VAR: "workflow level variable"
on:
  push:
jobs:
test_expressions:
  name: job to test ecpressions
  runs-on: ubutu-latest
  env:
    aBoolean: ${{ false }}
    aNull: ${{ null}}
    aNumber: ${{ 42 }}
    aString: this is a string
  steps:
    -name: test null
    if: (env.aNull == null)
    run: echo this variable is null
    -name: test boolean
    if:{{ from JSON(env.aBoolean) }}
    run: echo this job wont run as our variable is false
    -name: containd function
    if: ${{ contains(env.aString, 'this')}}
    run:
      echo "our string var contains the word this"
      echo "but here is a typo that will make this step fail"
    -name: test status check and format function
    if: ${ failure() }}
    env:
    FORMAT_VAR: ${{ format(GitHub {0} {1} {2}', 'actions', 'is', 'awesome!') }}
    run:
    echo "this step runs a special script to clean up failed step"
    echo ${{ env.FORMAT_VAR }}
    -name: function and always status check
    if: ${{ startsWith(env.eString, 'this')  && always() }}
    run: sleep 60
    -name: test cancel status check
    if: ${{ cancelled() }}
    run: echo " this job runs if a workflow is cancelled, it can also be used for clean -up script"
  test_variables:
    if:(github.event_name == 'pull_request')
  env:
    JOB_VAR: " job level variable"
    name: job to test variables
    runs-on: ubuntu-latest
    steps:
      -name: test varibles
      id: stepvar
      run:
        echo ${{ github.actor}}
        echo $GITHUB_JOB
        echo $WF_VAR
        echo $JOB_VAR
        echo $STEP_VAR
        echo ${{ vars.CONF_VAR}}
        echo ${{ secrets.A_SECRET}}

        
name: Manual workflow

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'Person to greet'
        # Default value if no value is explicitly provided
        default: 'World'
        # Input has to be provided for the workflow to run
        required: true
        # The data type of the input
        type: string

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  greet:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - name: Send greeting
      run: echo "Hello ${{ inputs.name }}"
